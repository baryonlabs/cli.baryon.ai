{
  "name": "@baryonlabs/edge",
  "version": "0.1.0",
  "description": "Baryon Edge (Enterprise) — run a pi/baryon agent in a network-isolated, read-only data sandbox: Postgres SELECT-only + MinIO/S3 GET-only via MCP, no data egress.",
  "type": "module",
  "bin": {
    "baryon-edge": "bin/baryon-edge.js"
  },
  "files": [
    "bin",
    "src",
    "Dockerfile",
    "docker-compose.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "keywords": [
    "baryon",
    "pi",
    "mcp",
    "enterprise",
    "air-gapped",
    "read-only",
    "data-egress",
    "postgres",
    "minio",
    "s3"
  ],
  "license": "UNLICENSED",
  "private": true
}
